// ? 全局不动配置项 只做导出不做修改

// * 首页地址（默认）
export const HOME_URL = '/consultation/index'

// * 默认路由name
export const HOME_ROUTER_NAME = 'consultationIndex'

// * 登录页地址（默认）
export const LOGIN_URL = '/login'

// * 默认主题颜色
export const DEFAULT_PRIMARY = '#409EFF'

// * Tabs（白名单地址，不需要添加到 tabs 的路由地址）
export const TABS_WHITE_LIST = ['/403', '/404', '/500', LOGIN_URL]
